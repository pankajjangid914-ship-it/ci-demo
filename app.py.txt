Python

# This is a simple Python web server using a tool called Flask.
from flask import Flask

app = Flask(__name__)

@app.route("/")
def hello_world():
    return "Hello, Python CI/CD World!"

# This part tells Render how to start our web server.
if __name__ == "__main__":
    app.run(host="0.0.0.0", port=10000)